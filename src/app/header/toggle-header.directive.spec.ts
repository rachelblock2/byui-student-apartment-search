import { ToggleHeaderDirective } from './toggle-header.directive';

describe('ToggleHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
