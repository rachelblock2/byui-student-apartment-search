import { AddModalDirective } from './add-modal.directive';

describe('AddModalDirective', () => {
  it('should create an instance', () => {
    const directive = new AddModalDirective();
    expect(directive).toBeTruthy();
  });
});
