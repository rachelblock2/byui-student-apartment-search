import { RemoveModalDirective } from './remove-modal.directive';

describe('RemoveModalDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveModalDirective();
    expect(directive).toBeTruthy();
  });
});
